@{title = "Ring 1 :: Pointer List Library"}
@{tableOfContents = "./api/ring1-library/_toc.ihtml"}
@(>"./_header.html.t")
  <section>
    <h2>Overview</h2>
    <p>The <em>Pointer List</em> library provides <em>a dynamically growing pointer lists</em>.</p>

    <p>The <em>Byte Buffer</em> library fulfils the <a href="@(baseurl)api/ring1-library/introduction.html">module concept</a>
    but does not require initialization.</p>
    
    <p>The include file of the <em>PointerList</em> module is <code class="inline">Ring1/PointerList.h</code>,
    its library file is <code class="inline">Ring1.lib</code>.</p>
    
    <h2>Sources and references</h2>
    <p>The <a href="@(repositoryUrl)/src/master">reference repository</a> provides non-authoritive
    <a href="@(repositoryUrl)/src/master/ring1/">sample implementations</a>.</p>
  </section>
@(>"./_footer.html.t")

@{title = "Ring 1 :: Result Library"}
@{tableOfContents = "./api/ring1-library/_toc.ihtml"}
@(>"./_header.html.t")
  <section>
    <h2>Overview</h2>
    <p>The <em>Result</em> library provides <em>result codes</em> denoting the outcome of an operations.</p>

    <p>The <em>Result</em> library fulfils the <a href="@(baseurl)api/ring1-library/introduction.html">module concept</a>
    but does not require initialization.</p>

    <p>The include file of the <em>Result</em> library is <code class="inline">Ring1/Result.h</code>,
    its library file is <code class="inline">Ring1.lib</code>.</p>
  </section>

  <section>
    <h2>Sources and references</h2>
    <p>The <a href="@(repositoryUrl)/src/master">reference repository</a> provides non-authoritive
    <a href="@(repositoryUrl)/src/master/ring1/">sample implementations</a>.</p>
  </section>
@(>"./_footer.html.t")

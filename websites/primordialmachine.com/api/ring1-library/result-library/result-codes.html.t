@{title = "Ring 1 :: Intrinsic Library"}
@{tableOfContents = "./api/ring1-library/_toc.ihtml"}
@(>"./_header.html.t")

<h3 id="result-codes">Result codes</h3>
<p>There are two result code values denoted by the symbolic constants <code class="inline">Ring1_Success</code>
and <code class="inline">Ring1_Failure</code>. The former denotes the success of an operation, the latter denotes
the failure of an operation. <code class="inline">Ring1_Success</code> is guaranteed to evaluate to zero value,
and <code class="inline">Ring1_Failure</code> is guaranteed to evaluate to a non-zero value.</p>

@(>"./_footer.html.t")

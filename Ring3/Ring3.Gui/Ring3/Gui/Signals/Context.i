/// The position (w.r.t. the parent's canvas coordinates) changed.
DEFINE(PositionChanged, "PositionChanged")

/// The size (w.r.t. the parent's canvas coordinates) changed.
DEFINE(SizeChanged, "SizeChanged")

/// The text of a widget changed.
DEFINE(TextChanged, "TextChanged")
